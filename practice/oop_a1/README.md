## TODO: 
**Create a folder with your name here and put you submission in that folder.** *Needless to say, you'll work on your respective branch*

## Tasks

### Task 1: Create a simple class
Create a class `packet` with:
- `addr`
- `data`
- `write`

Add a `display()` function to print values.

---

### Task 2: Create and use object
- Create an object of `packet` inside a module
- Assign values
- Call `display()`

---

### Task 3: Add constructor
Add `new()` in `packet` class to initialize values when object is created.

---

### Task 4: Create another class
Create a class `student` with:
- `name`
- `id`
- `marks`

Print details using a method.

---

### Task 5: Method with return value
Create a class `rectangle` with:
- `length`
- `width`

Add functions:
- `area()`
- `perimeter()`

---

### Task 6: Simple randomization
Create a class `transaction` with:
- `rand bit [7:0] addr`
- `rand bit [7:0] data`

Randomize and print 5 transactions.

---

### Task 7: Add simple constraint
In `transaction`, constrain:
- `addr` between `10` and `20`

